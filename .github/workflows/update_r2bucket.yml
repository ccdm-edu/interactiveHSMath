---
# Figure out static code files that have changed and copy/slam the new version into Cloudflare R2 bucket
# Dont have an existing action to delete from R2 bucket--do manually since no problem having junk files in R2 bucket
name: update_r2bucket
run-name: ${{ github.actor }} is updating R2 bucket for static code/public images

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  files-changed:
    name: detect what files changed
    runs-on: ubuntu-latest
    timeout-minutes: 3
    outputs:
      upload_file_str: ${{ steps.string_to_array.outputs.file_array }}
      run_the_upload: ${{ steps.changes.outputs.static }}
    steps:
      - run: echo "The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of the branch is ${{ github.ref }} and your repository is ${{ github.repository }}."

      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Paths Changes Filter
        uses: dorny/paths-filter@v3.0.2
        id: changes
        with:
          list-files: 'csv'
          filters: |
            static:
              - '**/static/css/**'
              - '**/static/images/**'
              - '**/static/js/**'

      - name: parse the string
        id: string_to_array
        shell: python
        # need a list of directories with a changed file since r2_upload_action only works at dir level
        #if:  steps.changes.outputs.static == 'true'
        run: |
          import os
          import pathlib
          #what we get from paths-filter is list of files that changed from specified directories of interest
          #str_ver = "${{ steps.changes.outputs.static_files }}"
          str_ver = "DjangoProto/InteractiveMath_prj/static/css/FakeCSStestFile.css,DjangoProto/InteractiveMath_prj/static/js/FakeJStestFile.js,DjangoProto/InteractiveMath_prj/static/js/FakeJStestFile2.js,DjangoProto/InteractiveMath_prj/static/css/FakeCSStestFile2.css"
          print(f'this is what the first job puts out: {str_ver}')
          array_of_files = str(str_ver).split(',')
          array_of_dir_path = []
          json_array_of_files = '{"file_array":['                  
          for i in array_of_files:
            currPath = pathlib.Path(i)
            currDir = currPath.parent
            if currDir not in array_of_dir_path:
              array_of_dir_path.append(currDir)
          for i in array_of_dir_path:
            print(f'dir = {i}')
            json_array_of_files = json_array_of_files + '"' + str(i) + '",'
          #remove last extra , and close off json format
          json_array_of_files = json_array_of_files[:-1] + ']}'
          print(f'string_to_array parsing to json format is {json_array_of_files} ')
          with open(os.environ['GITHUB_OUTPUT'], "a") as f:
            f.write(f"file_array={json_array_of_files}")
          

  test_file_array: 
    # test we got the array ok
    name: delete me when tested, look at results before used
    runs-on: ubuntu-latest
    needs: [files-changed]
    steps:
      - name: test results
        id: test_results
        #if: ${{ needs.files-changed.outputs.run_the_upload == 'true' }} 
        run: |
          echo 'the run-the-upload value is ${{ needs.files-changed.outputs.run_the_upload }} '
          echo 'we read the results of previous job as ${{ needs.files-changed.outputs.upload_file_str }}'

  loop-thru-files:
    strategy: 
      matrix:
        files2use: ${{ fromJSON(needs.files-changed.outputs.upload_file_str).file_array }}
    runs-on: ubuntu-latest
    needs: [files-changed]
    steps: 
      - name: did we get array
        id: check array
        run: |
          echo "We have file meeting the criterion ${{ matrix.files2use }}"



          
  push_new_files_up:
    #if: ${{ needs.files-changed.outputs.run_the_upload == 'true' }} 
    if: 'false'
    runs-on: ubuntu-latest
    needs: [files-changed]
    steps:

      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: push dir to CF R2 bucket
        uses: ryand56/r2-upload-action@latest
        with:
          r2-account-id: ${{ secrets.R2_ACCOUNT_ID }}
          r2-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          r2-bucket: ${{ secrets.R2_BUCKET }}
          source-dir: DjangoProto/InteractiveMath_prj/static/css/
          destination-dir: css/
          keep-file-fresh: 'true'
    


          
...

